#include <iostream>
#include <map>

int	main(int ac, char **av) {

    std::cout << "ok" << std::endl;
try {
  // Demarre Client
    // Run le tout
} catch (const std::exception &msg) {
		  //gestion erreur
	}
}